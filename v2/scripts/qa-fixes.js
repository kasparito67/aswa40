(()=>{
  // Phase 4: route navigation, trackpad gestures and Re.Watched layout are now
  // owned by the canonical renderer in app.js. Kept as a temporary no-op until
  // Phase 5 removes obsolete late-load patches from index.html.
})();
